# Local scaffold implementation plan

**Goal:** Create the user-approved local ReSTIR repository scaffold.

**Architecture:** Separate RTXDI and Falcor integration slots share scene records, evaluation conventions, and writing sources. Dependencies remain uninstalled until selected.

**Tech stack:** Git, Markdown, JSON-compatible YAML, CSV, Python standard library, supplied LaTeX templates.

**Spec:** [Accepted layout](../../decisions/0001-project-layout.md).

## Constraints

Preserve course originals. Do not import sibling projects. Limit geometry work to supporting tasks and omit Blender plugin development. Do not invent dependencies, results, authors, or license selection. No remote creation or publication.

## Execution

- [x] Inspect destination, parent guidance, and supplied files.
- [x] Create documented directories, records, templates, and provenance.
- [x] Add a standard-library structural check and lightweight CI.
- [x] Verify links, source hashes, ignore rules, and document compilation.
- [x] Initialize Git and record final verification.

The user approved implementation in this task. This reversible scaffold needs structural validation; renderer tests will be added when rendering code exists.
